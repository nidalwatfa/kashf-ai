# مكتبات معالجة البيانات والتحليل
pandas>=2.0.0
numpy>=1.24.0

# مكتبات تعلّم الآلة (ML)
scikit-learn>=1.3.0

# مكتبات معالجة اللغات الطبيعية (NLP)
# إذا كان المشروع يركز على اللغة العربية، هذه المكتبات ضرورية:
nltk>=3.8.1
# يمكن إضافة مكتبات مثل Arabic-LTP أو Ar-BERT إذا كانت مستخدمة

# مكتبات الرسوم البيانية والتحليل البصري
matplotlib>=3.7.2
seaborn>=0.13.0

# إذا كنت تستخدم نماذج عميقة (Deep Learning) مثل TensorFlow/PyTorch
# (يجب إزالة هذه السطور إذا لم تكن تستخدم أي منهما)
# tensorflow>=2.15.0
# torch>=2.0.0
# transformers>=4.35.0

# مكتبات أخرى مفيدة
tqdm>=4.66.1  # لطباعة شريط التقدم أثناء العمليات الطويلة
joblib>=1.3.2 # لحفظ وتحميل النماذج
